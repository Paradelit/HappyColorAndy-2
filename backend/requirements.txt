# Requiere Python 3.10 - 3.12 (recomendado 3.12).
# Python 3.13/3.14 aun no tienen wheels para algunas de estas libs y pip
# intentaria compilar desde fuente (falla en Windows). Usa 3.12.
#
# Pines relajados (>=) para que pip elija la wheel compatible con tu Python.
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pillow>=10.3
opencv-python-headless>=4.9
scikit-image>=0.22
scikit-learn>=1.4
numpy>=1.26,<2.3
scipy>=1.11
shapely>=2.0
