# Compatible con Python 3.10 - 3.14 (sin OpenCV, que aun no tiene wheels 3.14).
# Pines relajados (>=) para que pip elija la wheel compatible con tu Python.
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pillow>=10.3
scikit-image>=0.22
scikit-learn>=1.4
numpy>=1.26
scipy>=1.11
shapely>=2.0
# Modo "ilustracion (IA)" foto->dibujo (Gemini / Nano Banana). Se activa en
# tiempo de ejecucion solo si STYLIZE_API_KEY esta definida.
google-genai>=0.3
# Alternativa de pago (OpenAI): descomenta si usas STYLIZE_PROVIDER=openai
# openai>=1.30
